import { useTimer } from 'react-timer-hook';

export default function Timer({}) {
  return (
    <div className='flex justify-center text-gray-400 rounded-md bg-zinc-900 w-80'>
      <p>Timer</p>
    </div>
  );
}
